{
	"name": "nipahtv",
	"version": "1.0.0",
	"description": "Better Kick and 7TV Emote Integration for Kick",
	"author": "Xzensi",
	"license": "AGPL-3.0-only",
	"scripts": {
		"build": "rm -f dist/client.user.js && cat compile_header >> dist/client.user.js && esbuild src/app.js --bundle >> dist/client.user.js"
	},
	"devDependencies": {
		"esbuild": "0.20.0",
		"semver": "^7.6.0"
	}
}
